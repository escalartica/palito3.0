import 'package:flutter/material.dart';

class AppShadows {
  static const BoxShadow cardShadow = BoxShadow(
    color: Color(0x0D000000), // Negro muy suave con opacidad
    blurRadius: 10,
    offset: Offset(0, 4),
  );
}