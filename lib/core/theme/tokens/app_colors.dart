import 'package:flutter/material.dart';

class AppColors {
  // Colores base (nuestra paleta de marca)
  static const Color primary = Color(0xFFE67E22); // Un naranja cálido, elegante, no chillón
  static const Color background = Color(0xFFF9F9F7); // Un blanco roto, muy "papel" de diario
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF2C3E50); // Un gris muy oscuro, más suave que el negro puro
  static const Color textSecondary = Color(0xFF7F8C8D);
  
  // Colores para estados
  static const Color error = Color(0xFFE74C3C);
  static const Color success = Color(0xFF27AE60);
}